pipeline {
    agent any

    tools {
        jdk 'JAVA_HOME'
        maven 'M2_HOME'
    }

    environment {
        // Docker
        DOCKER_IMAGE = "spring-boot-app"
        DOCKER_TAG = "${BUILD_NUMBER}"

        // Kubernetes
        KUBE_NAMESPACE = "devops"
        KUBE_DEPLOYMENT = "spring-app"

        // Projet
        PROJECT_DIR = "student-management"
    }

    stages {
        stage('📥 Checkout Code') {
            steps {
                echo "========== CHECKOUT CODE =========="
                git branch: 'main', url: 'https://github.com/shaymaesprit/DevopsShaymaSAE7.git'
                sh 'ls -la'
            }
        }

        stage('🏗️ Build Maven') {
            steps {
                echo "========== BUILD MAVEN =========="
                dir("${PROJECT_DIR}") {
                    sh 'mvn clean package -DskipTests'
                }
            }
        }

        stage('🔍 SonarQube Analysis') {
            steps {
                echo "========== SONARQUBE ANALYSIS =========="
                dir("${PROJECT_DIR}") {
                    withCredentials([string(credentialsId: 'sonar', variable: 'SONAR_TOKEN')]) {
                        withSonarQubeEnv('sonar') { // le nom exact du serveur dans Jenkins
                            sh """
                                mvn sonar:sonar \
                                    -Dsonar.projectKey=student-management \
                                    -Dsonar.projectName=Student-Manage
